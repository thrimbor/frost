const MULTIBOOT_HEADER_MAGIC = &h1BADB002          '// the "magic-number" of the multiboot header
const MULTIBOOT_HEADER_MODULES_ALIGNED = &h00000001
const MULTIBOOT_HEADER_GET_MEMORY = &h00000002
const MULTIBOOT_HEADER_GET_VBE = &h00000004
const MULTIBOOT_HEADER_GET_ADDR = &h00010000
